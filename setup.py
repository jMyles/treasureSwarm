from setuptools import setup, find_packages

setup(name='treasureSwarm',
      version='0.0.0',
      description='The network is a goldmine, swirling all around.',
      packages=['treasure_swarm'])
